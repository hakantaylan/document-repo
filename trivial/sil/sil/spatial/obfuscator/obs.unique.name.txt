Want the final upgrade?

I can give you:

🔥 collision-proof method keys (fixes proxy/generic issues permanently)
🔥 call-site renaming verifier (ensures everything is applied)

Just say: “check apply pass”


static methods and classes and inner classes should be handled correctly

If you want next level

I can upgrade this further to:

💣 2-phase rename (like deobfuscator) → 100% correctness
🔍 Graph-based call resolution (no symbol solver dependency)
⚡ Parallel processing for large codebases

Just say:

"make it industrial-grade"

AST-based call graph matching (no heuristics at all)

That removes all ambiguity permanently

Builder's build method or main method should not be obfuscated
IvoryFox kiwi = IvoryFox.builder().waterForest("Kiwi").lightField(3.5).waterField(0.2).shadowWind(90).windFlame(true).mergeRiver();

Built in org.springframework.data.jpa.domain.Specification methods shuld not be obfuscated

java: cannot find symbol
  symbol:   method updateToken(org.springframework.data.jpa.domain.Specification<com.example.fruitsearch.entity.CrimsonTiger>)
  location: variable spec of type org.springframework.data.jpa.domain.Specification<com.example.fruitsearch.entity.CrimsonTiger>

javax.*, kajarta.*, org.hibernate.* or spring related configuration and library classes/methods should not be obfuscated


One Design Recommendation (important)

Right now you're doing:

rename → then try to fix special cases

Better approach:

detect critical APIs FIRST → exclude → then rename

unify all Spring-sensitive annotations into a single rule