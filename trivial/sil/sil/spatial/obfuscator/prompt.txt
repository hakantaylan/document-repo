You are fixing a Java 25 project that implements a JavaParser-based obfuscator/deobfuscator.

Your goal:
Make ALL tests pass by correcting the architecture of SafeTransformVisitor and related symbol-handling code.

---

## CORE PROBLEM

Current implementation mutates AST (renames) while still relying on symbol resolution.
This breaks JavaParser resolution and causes inconsistent renaming.

---

## MANDATORY ARCHITECTURE (DO NOT DEVIATE)

Implement a strict 3-phase pipeline:

PHASE 0 — Preprocessing

* Detect Lombok annotations (existing logic)
* Rename classes FIRST (so resolution remains correct)

PHASE 1 — COLLECT (NO MUTATION)

* Collect mappings for:

  * methods
  * fields
  * constructors (optional but safe)
* DO NOT rename anything here
* ONLY populate maps

PHASE 2 — APPLY DECLARATIONS

* Rename:

  * MethodDeclaration
  * FieldDeclaration / VariableDeclarator (only fields)
  * ConstructorDeclaration (if implemented)

PHASE 3 — APPLY USAGES

* Rename:

  * MethodCallExpr
  * MethodReferenceExpr
  * FieldAccessExpr
  * ObjectCreationExpr
  * ClassOrInterfaceType
  * LambdaExpr (if needed)

---

## CRITICAL RULES (VERY IMPORTANT)

1. NEVER GENERATE NEW NAMES DURING APPLY PHASE
   WRONG:
   if (obfName == null) generate new name

   CORRECT:
   if (obfName != null) apply rename
   else DO NOTHING

2. ALWAYS USE CONSISTENT KEYS

   METHOD KEY:
   use ResolvedMethodDeclaration.getQualifiedSignature()

   FIELD KEY:
   fqcn + "." + fieldName

   CONSTRUCTOR KEY:
   use resolved.getQualifiedSignature()

3. NEVER MUTATE DURING COLLECTION

4. ALWAYS RESOLVE BEFORE MUTATION
   (this is why 2-phase is required)

5. ONLY RENAME INTERNAL CLASSES
   Use package prefix check

6. HANDLE Optional CORRECTLY
   getScope() returns Optional<Expression>, must unwrap

7. USE CORRECT API:

   * MethodCallExpr → setName()
   * MethodReferenceExpr → setIdentifier()

---

## REQUIRED METHODS TO IMPLEMENT

1. collectMethodMappings(CompilationUnit)

2. collectFieldMappings(CompilationUnit)

   * only if resolved.isField()

3. collectConstructorMappings(CompilationUnit) (optional but safe)

4. buildResolvedMethodKey(ResolvedMethodDeclaration)

5. Fix apply() method to use 3-phase pipeline

---

## LEXICAL PRESERVATION (FOR TESTS)

Fix SymbolSolverFactory:

* Enable LexicalPreservingPrinter.setup(cu)
* Replace cu.toString() with LexicalPreservingPrinter.print(cu)

This is REQUIRED for formatting tests.

---

## EXPECTED TEST FIXES

This must fix:

* methodCallIsUpdated
* privateMethodIsRenamed
* staticUtilityMethodWorks
* staticFactoryMethodWorks
* multipleRenamesInSameFile
* lexicalPreservingPrinterPreservesFormatting
* lexicalPreservingPrinterPreservesCustomIndentation

---

## ANTI-PATTERNS TO REMOVE

* String-based fallback key building using AST types
* Generating names during visitMethodCallExpr
* Renaming before resolution completes
* Using Optional like a direct node (isNameExpr on Optional)

---

## OUTPUT FORMAT

Provide:

1. Updated SafeTransformVisitor (full file)
2. Updated apply() method
3. All new helper methods
4. Minimal changes to SymbolSolverFactory

DO NOT explain. Only provide final code.
